# Frontend environment template — copy to .env. (.env is gitignored.)
# Vite only exposes vars prefixed with VITE_ to the client.

# Base URL of the backend REST API.
VITE_API_URL=http://localhost:4000/api
